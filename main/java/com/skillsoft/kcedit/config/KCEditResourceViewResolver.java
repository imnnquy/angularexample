package com.skillsoft.kcedit.config;

import org.springframework.web.servlet.view.InternalResourceViewResolver;

/**
 * This is a wrapper of <code>InternalReourceViewResolver</code><br/>
 * Currently, we don't need to change this
 *
 */
public class KCEditResourceViewResolver extends InternalResourceViewResolver {

    /**
     * Default constructor
     */
    public KCEditResourceViewResolver() {
        super();
    }
}
